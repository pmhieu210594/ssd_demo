<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdd.platform.infrastructure.persistence.mapper.RoleMapper">

    <resultMap id="roleResultMap" type="Role">
        <id column="role_id"
            property="roleId"
            javaType="java.util.UUID"
            typeHandler="com.sdd.platform.infrastructure.persistence.typehandler.UUIDTypeHandler"/>
        <result column="role_name" property="roleName"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="delete_flag" property="deleteFlag"/>
        <result column="status" property="status"/>
    </resultMap>

    <sql id="columns">
        role_id, role_name, description, created_at, created_by, updated_at, updated_by, delete_flag, status
    </sql>

    <sql id="roleStatusFilter">
        <choose>
            <when test="status != null and status == 'ACTIVE'">
                AND delete_flag = 0
                AND status = 'ACTIVE'
            </when>
            <when test="status != null and status == 'DELETED'">
                (delete_flag = 1 OR status = 'DELETED')
            </when>
            <otherwise>
            </otherwise>
        </choose>
    </sql>

    <select id="findActive" resultMap="roleResultMap">
        SELECT <include refid="columns"/>
        FROM tbl_dim_role
        <where>
            <include refid="roleStatusFilter"/>
            <if test="keyword != null and keyword != ''">
              AND LOWER(role_name) LIKE CONCAT('%', LOWER(#{keyword}), '%')
            </if>
        </where>
        ORDER BY role_name ASC, role_id ASC
    </select>

    <select id="findActiveById" resultMap="roleResultMap">
        SELECT <include refid="columns"/>
        FROM tbl_dim_role
        WHERE role_id = #{roleId}
          AND delete_flag = 0
          AND status = 'ACTIVE'
    </select>

    <select id="findById" resultMap="roleResultMap">
        SELECT <include refid="columns"/>
        FROM tbl_dim_role
        WHERE role_id = #{roleId}
    </select>

    <select id="existsActiveName" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM tbl_dim_role
            WHERE delete_flag = 0
              AND status = 'ACTIVE'
              AND LOWER(role_name) = LOWER(#{roleName})
              <if test="excludeRoleId != null">
                  AND role_id &lt;&gt; #{excludeRoleId}
              </if>
        )
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO tbl_dim_role (
            role_id,
            role_name,
            description,
            created_at,
            created_by,
            updated_at,
            updated_by,
            delete_flag,
            status
        ) VALUES (
            #{roleId},
            #{roleName},
            #{description},
            #{createdAt},
            #{createdBy},
            #{updatedAt},
            #{updatedBy},
            0,
            #{status}::record_status
        )
    </insert>

    <update id="update">
        UPDATE tbl_dim_role
        SET role_name = #{roleName},
            description = #{description},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE role_id = #{roleId}
          AND delete_flag = 0
          AND status = 'ACTIVE'
    </update>

    <update id="logicalDelete">
        UPDATE tbl_dim_role
        SET delete_flag = 1,
            status = 'DELETED'::record_status,
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE role_id = #{roleId}
          AND delete_flag = 0
          AND status = 'ACTIVE'
    </update>

</mapper>
