# Sources — SDD-LEAD-TIME

| source | type | read depth | trust level | summary |
|---|---|---|---|---|
| `docs/changes/SDD-LEAD-TIME/01_raw-input.md` | Ticket body | Full | High | Primary requirement source: show `spec-pack.md` created time and `report.md` updated time in `pm-dashboard` Ticket Detail drawer; persist to `tbl_dim_ticket.started_at` (existing) / `completed_at` (new); `-` fallback if file missing; i18n required. |
| `docs/standards/templates/_ticket-template/` (all files, esp. `spec-pack.md`, `report.md`) | Template | Full | High | Confirms `spec-pack.md` and `report.md` both carry a `- **Tạo ngày:** {DATE_YYYY_MM_DD}` header-metadata line before the first `##` heading. `report.md` has no dedicated "updated" field. |
| `docs/changes/GIT-PR-METADATA-COLLECTOR/spec-pack.md` | Existing spec-pack (pattern reference) | Full | High | Used as the structural/format model for this spec-pack: 18 numbered sections, ID prefixes `BR-`, `AC-`, `H-`, `A-`, `OI-`. |
| `docs/architecture/overview.md` | Architecture doc | Full | High | Confirms hexagonal BE layering, FE state-management rules (TanStack Query/Redux/Zustand), and that Artifact Scanner is the existing markdown-ingestion entry point. |
| `.claude/rules/00-safety.md`, `10-style.md`, `20-architecture.md`, `30-security.md`, `40-testing.md` | Operating rules | Full | High | Constrain Phase 1 output to `.claude/`/`docs/` only; hexagonal layering; naming/style conventions for any future implementation. |
| `EDCAP_BE/src/main/java/com/sdd/platform/domain/service/markdown/core/MarkdownParserCore.java` | Backend source | Full | High | `extractHeaderMetadata()` parses top-of-file `- **Key:** Value` lines (before first `##`) into a normalized-key map — this is the mechanism that can supply the "created time" value from `spec-pack.md`'s `Tạo ngày` line. |
| `EDCAP_BE/src/main/java/com/sdd/platform/domain/service/markdown/specpack/SpecPackMarkdownParser.java` | Backend source | Excerpt (structure, `ParsedArtifact` shape, front-matter/ticket-id inference) | High | Confirms parser currently exposes `sourcePath`, `ticketId`, `parseStatus`, `artifactStatus`, `sections`, `tables`, `acceptanceCriteria`, `contentHash`, `parsedSummary` — no created-date field yet. |
| `EDCAP_BE/src/main/java/com/sdd/platform/domain/service/markdown/report/ReportMarkdownParser.java` | Backend source | Excerpt (structure, `ParsedArtifact` shape) | High | Same shape as spec-pack parser; extracts `acceptedRisks` table; no updated-date field yet. |
| `EDCAP_BE/src/main/java/com/sdd/platform/application/usecase/scanner/ArtifactScannerService.java` | Backend source | Excerpt (constants, parse/persist call sites) | High | Confirms `spec-pack.md`/`report.md` filenames are scanned per ticket folder, parsed via the two parsers above, and persisted via `persistSpecPackParse`/`persistReportParse`. This is the natural place to also persist `started_at`/`completed_at` onto `tbl_dim_ticket` each scan. |
| `EDCAP_BE/src/main/java/com/sdd/platform/infrastructure/github/GithubArtifactScannerSourceAdapter.java` | Backend source | Full | High | Confirms file content is fetched via GitHub Trees/Blobs API (no filesystem `stat()`); has a `resolveCommitDate` helper for whole-revision commit dates but nothing for per-file commit history — ruled out as the timestamp source per user decision (dates come from file content instead). |
| `EDCAP_BE/src/main/resources/db/migration/V4__init_shema_v2.sql` (lines 174–193) | DB migration | Full (table definition) | High | `tbl_dim_ticket` already has `started_at TIMESTAMPTZ` (nullable) and no `completed_at`. Confirms exact column list/constraints to mirror for the new column. |
| `EDCAP_BE/src/main/resources/db/migration/` (directory listing) | DB migration | Filenames only | High | Latest migration is `V512__add_ai_finding_stat_tracking.sql`; next version for this ticket is `V513`. |
| `EDCAP_BE/src/main/java/com/sdd/platform/infrastructure/persistence/adapter/PmDashboardJdbcAdapter.java` | Backend source | Excerpt (query referencing `started_at`) | High | Confirms `started_at` is already selected/ordered by the PM Dashboard ticket-detail query — establishes the pattern for adding `completed_at`. |
| `EDCAP_FE/src/pages/pm-dashboard/components/TicketDetailDrawer.tsx` | Frontend source | Excerpt (`TicketInformationCard`, ~line 236) | High | Confirms existing `createAt`/`updatedAt`/`mergedAt` rendering pattern: `t("Pages.PmDashboard.<key>", { defaultValue })` + `formatDateTime(...)`, with `mergedAt` rendered conditionally — the pattern to replicate for the two new fields and the `-` fallback. |
| Grep for `lead time`/`leadtime`/`LeadTime` (case-insensitive, whole repo) | Codebase search | Full | High | Only match is the ticket's own raw-input file — confirms no pre-existing DORA lead-time feature; this ticket is a narrower "surface two artifact timestamps" feature despite its name. |

## Not read in Phase 1 (deferred to Phase 3 if needed)

- Full body of `ArtifactScannerService.java` beyond scan/parse/persist call sites (full transaction-per-item flow).
- Full body of `PmDashboardJdbcAdapter.java` beyond the `started_at` reference (full ticket-detail SQL/joins, DTO mapping).
- GitHub webhook signature verification code (not relevant — no webhook change needed for this ticket).
- Other artifact parsers (`selfreview`, `reviewchecklist`, `aireviewstats`) — not impacted by this ticket.
