# Open Issues

**Ticket ID**: PROMPT_TEMPLATE_REUSE_RATE
**Create date**: 2026-08-10
**Author**: SDD Analyst
**Update date**: 2026-08-17 (OI-9 raised during self-review; OI-10 raised during Phase 3 review; OI-11/OI-12 raised during Phase 5 human review round 1; OI-13/OI-14 raised during Phase 5 human review round 2; OI-15/OI-16/OI-17 raised during Phase 5 Codex automated review round 3 — OI-15 accepted as risk, OI-16/OI-17 resolved)

| ID | issue | phase | impact | severity | owner | due date | status |
|---|---|---|---|---|---|---|---|
| OI-PROMPT_TEMPLATE_REUSE_RATE-1 | Statistics API scope was ambiguous | Phase 3 (Impl Plan), Phase 8 (FE/BE Contract) | FE/BE Contract, API design | High | Product/Tech Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-2 | No existing table cleanly fit `total_check_count`/`template_match_count` per phase | DB/Migration Impact | DB schema, migration V510 | High | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-3 | `tbl_dim_artifact_type` missing rows for `OPEN_ISSUES`, `CONTEXT`, `CODEX_REVIEW` | DB/Migration Impact, Acceptance Criteria | Data completeness of phase statistics | Medium | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-4 | Raw requirement's validation-scope path/API path did not literally match repo/platform convention | Backend validation logic, API contract | Correctness of file matching; contract consistency | High | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-5 | Raw requirement's Phase 3 file name (`impl_plan_template.md`) mismatched actual convention | Phase-file mapping accuracy | Medium | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-6 | `ConnectorOrchestrator` documented in `service-layer-map.md` does not exist in source | None on this ticket directly | Documentation accuracy (architecture doc may be stale/aspirational) | Low | Architecture doc owner | — | Accepted, no action |
| OI-PROMPT_TEMPLATE_REUSE_RATE-7 | No decision on which revision to read the *template* file content from | Backend comparison logic | Correctness when a PR itself edits a template file | Medium | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-8 | No decision on failure handling when a single file's GitHub blob/tree fetch fails mid-webhook | Backend validation logic, error handling | Webhook reliability | Medium | Backend Lead | — | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-9 | Confirm whether the new template usage statistics endpoint should be accessible to all authenticated dashboard users or restricted to PM-role users | Security/Privacy Impact, API contract | Medium | Product/Tech Lead | 2026-08-10 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-10 | `usageRate` rounding rule was undefined (`templateMatchCount / totalCheckCount * 100` alone yields inconsistent display precision, e.g. `66.6666666667%` vs `66.7%` vs `67%`) | DTO/Schema Impact, FE display | Low | Product/Tech Lead | 2026-08-11 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-11 | Shipped `findTemplateUsage` had no phase filter, so the response included phases with no mapped artifact type (`0-A`, `0-B`, `9`, `10`), contradicting spec-pack's own Terminology/Boundary Values (8 in-scope phases) — raised in Phase 5 human review round 1 | API contract, dashboard UX | High | Product/Tech Lead | 2026-08-11 | Superseded by OI-13 (fix was insufficient) |
| OI-PROMPT_TEMPLATE_REUSE_RATE-12 | Shipped `TemplateUsageByPhase.tsx` UI text (section title/description, table headers) had no locale keys in `en`/`vi`/`ja`, only hard-coded English `defaultValue` fallbacks — raised in Phase 5 human review round 1 | i18n compliance, dashboard UX | Medium | Frontend Lead | 2026-08-11 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-13 | OI-11's `WHERE EXISTS (... tbl_dim_artifact_type ...)` fix still let phase `0-A` leak into the response, because that table is shared with an unrelated feature (AI-safety file scanning) that maps 3 artifact types to `0-A` — raised in Phase 5 human review round 2 | API contract, dashboard UX | High | Product/Tech Lead | 2026-08-11 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-14 | Shipped `validateTemplateUsage(...)` ran unconditionally on every supported `pull_request` webhook action (`opened`/`synchronize`/`reopened`/`closed`); since each delivery re-reads the PR's full cumulative diff and `recordCheck()` is a pure additive counter, this duplicated/inflated counts once per lifecycle event instead of once per PR — raised in Phase 5 human review round 2 | Statistics correctness | High | Backend Lead | 2026-08-11 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-15 | GitHub webhook delivery is at-least-once transport; if GitHub redelivers the identical `action=closed`+`merged=true` event for the same PR, `validateTemplateUsage(...)` has no idempotency key to detect the repeat, so counters would be double-counted — raised by Codex automated review, Phase 5 round 3 | Statistics correctness, rare-occurrence risk | Medium | Product/Tech Lead | — | Accepted Risk — no code fix |
| OI-PROMPT_TEMPLATE_REUSE_RATE-16 | Shipped `validateTemplateUsage(...)` wrapped the shared, PR-level `resolveRevision`/`listTree` calls in a blanket `catch (Exception ex)` that logged a `warn` and returned for any failure, including `5xx`/timeout/non-HTTP errors, unlike the already-classified per-file blob-fetch calls — raised by Codex automated review, Phase 5 round 3 | Webhook reliability, silent data-quality gap | High | Backend Lead | 2026-08-17 | Resolved |
| OI-PROMPT_TEMPLATE_REUSE_RATE-17 | `TemplateUsageStatWriter.recordIndependently(...)` had no try/catch at its call site; `@Transactional(REQUIRES_NEW)` isolates the transaction but does not swallow exceptions, so a `DataAccessException` writing one file's counters aborted the entire webhook, contradicting the documented Best-Effort Audit/Stat Write Pattern — raised by Codex automated review, Phase 5 round 3 | Webhook reliability, best-effort-pattern violation | High | Backend Lead | 2026-08-17 | Resolved |

## Blockers

None remaining — all decisions needed to finalize the FE/BE Contract Impact and DB/Migration Impact sections of `spec-pack.md` have been resolved (see Resolution Log).

## Questions

None remaining — see Resolution Log.

## Pending Human Decisions

None remaining — HD1–HD8 all resolved in `03_source-availability.md` → Resolved Decisions; OI-9 through OI-14, OI-16, OI-17 resolved below (OI-11 superseded by OI-13); OI-15 accepted as risk (no fix required).

## Accepted but Unresolved Items

- OI-PROMPT_TEMPLATE_REUSE_RATE-6 (`ConnectorOrchestrator` doc/source mismatch) is accepted as out of scope for this ticket's implementation; no action taken. Recorded here since it surfaced during source investigation for this ticket.
- OI-PROMPT_TEMPLATE_REUSE_RATE-15 (webhook-redelivery double-counting) is accepted as an unmitigated risk; see Resolution Log entry for reasoning and decision-maker.

## Resolution Log

| Date | ID | Resolution | By |
|---|---|---|---|
| 2026-08-10 | OI-1 | Display moves from `TicketDetailDrawer.tsx` to `PMDashboardPage.tsx` (rendered right after `AllTicketsTable`); statistics are scoped by project + repository, matching the page's existing `filters.projectId`/`filters.repositoryId` | Stakeholder (via chat) |
| 2026-08-10 | OI-2 | New dedicated table `tbl_fact_template_usage_stat` with columns `project_id, repository_id, phase_id, total_check_count, template_match_count` plus standard audit columns (`id`, `created_at`, `updated_at`) | Stakeholder (via chat) |
| 2026-08-10 | OI-3 | Missing `tbl_dim_artifact_type` rows (`OPEN_ISSUES`, `CONTEXT`, `CODEX_REVIEW`) added in scope, via the same `V510` migration | Stakeholder (via chat) |
| 2026-08-10 | OI-4 | Re-checked against the actual `ticketKeyFromPath()` implementation (`GithubWebhookService.java:434-449`): it already does a `/changes/` substring search, not a literal fixed prefix, so it transparently handles the real repo layout — no separate path-convention fix needed, the new validation-scope check reuses this same method/approach. The API URL part is resolved via OI's own HD5 → `/api/v1/pm/dashboard/template-usage`, added to the existing `PmDashboardController`/`PmDashboardService` (no new controller/service class). Still relevant/necessary at the backend level regardless of the FE display-location change (OI-1), since per-ticket/phase attribution still happens before project/repository-level aggregation | Stakeholder (via chat) + code evidence |
| 2026-08-10 | OI-5 | Confirmed: implementation uses `impl-plan.md` (actual convention), not the raw requirement's `impl_plan_template.md` | Stakeholder (via chat) |
| 2026-08-10 | OI-6 | Accepted as out of scope; no action | Stakeholder (via chat) |
| 2026-08-10 | OI-7 | Template file is read from the same PR head revision as the changed file | Stakeholder (via chat) |
| 2026-08-10 | OI-8 | Reuse the existing `isSkippableBlobError` pattern (`GithubSecurityEvidenceSnapshotService.java:606-609`): `401/403/404` → skip that file, log `warn`, continue; other errors → propagate/fail as today | Stakeholder (via chat) + code evidence |
| 2026-08-10 | OI-9 | Restricted to PM-role users. Reuse the existing `PmDashboardService.requirePm(caller, projectId)` gate (`PmDashboardService.java:197-212`, already used by every other PM Dashboard read method — options/summary/tickets/detail) on the new `/api/v1/pm/dashboard/template-usage` endpoint; ADMIN system role passes, otherwise caller must hold the PM project role for the given `projectId` | Stakeholder (via chat) + code evidence |
| 2026-08-11 | OI-10 | `usageRate` is computed server-side as `templateMatchCount / totalCheckCount * 100`, rounded to exactly 1 decimal place (e.g. `66.7`, `100.0`, `0.0`), returned as a `Double`/`number`; when `totalCheckCount == 0`, the API returns `usageRate: null` and the FE renders `"-"` (never `0`, `0.0`, or `NaN`). The FE appends the `%` sign at render time; the API does not include it in the numeric value | Stakeholder (via chat) |
| 2026-08-11 | OI-11 | Excluded (initially). `PmDashboardJdbcAdapter.findTemplateUsage` was scoped with `WHERE EXISTS (SELECT 1 FROM tbl_dim_artifact_type at WHERE at.phase_id = ph.phase_id)` — data-driven per Business Rule #7, no hard-coded phase-code list. **Later found insufficient**: `0-A` carries 3 `tbl_dim_artifact_type` rows for an unrelated feature (AI-safety file scanning), so it still passed this `EXISTS` filter and leaked into the response. Superseded by OI-13 | Human reviewer (Phase 5 rejection, round 1) |
| 2026-08-11 | OI-12 | Added `Pages.PmDashboard.templateUsageByPhase`, `.templateUsageByPhaseDescription`, and `.templateUsage.{phase,totalChecked,matched,usageRate}` keys to `EDCAP_FE/public/locales/{en,vi,ja}/locale.json`; removed the `defaultValue`-only fallback usage in `TemplateUsageByPhase.tsx` now that real keys exist. Codified as Business Rule #9 in `spec-pack.md` to prevent recurrence | Human reviewer (Phase 5 rejection, round 1) |
| 2026-08-11 | OI-13 | Resolved. Replaced the `EXISTS`/JOIN filter (OI-11) with a literal `WHERE ph.phase_code IN ('1','2','3','4','5','6','7','8')` in `PmDashboardJdbcAdapter.findTemplateUsage`, grounded directly in the already-fixed Terminology phase scope rather than an indirect `tbl_dim_artifact_type` proxy that is shared across unrelated features. `PmDashboardJdbcAdapterFindTemplateUsageTest` rewritten to assert the literal `IN` clause and the absence of `0-A`/`EXISTS`. Business Rule #7 in `spec-pack.md` clarified to scope this exception (phase-set boundary vs. file-to-phase mapping) | Human reviewer (Phase 5 rejection, round 2) |
| 2026-08-11 | OI-14 | Resolved. `GithubWebhookService.handlePullRequest` now gates the `validateTemplateUsage(...)` call on `"closed".equals(action) && pullRequest.path("merged").asBoolean(false)`, so recording happens exactly once per PR (at close+merge) instead of on every `opened`/`synchronize`/`reopened`/`closed` delivery. `GithubWebhookServiceTest` updated (existing template-usage tests now use `action=closed, merged=true`) plus 2 new tests asserting `recordIndependently` is never called for `opened`/`synchronize`/`reopened` actions or for a `closed`-but-not-merged PR. Business Rule #11 and Terminology's `total_check_count` definition updated in `spec-pack.md` | Human reviewer (Phase 5 rejection, round 2) |
| 2026-08-17 | OI-15 | **Accepted Risk — no code fix.** Ticket owner declined to add webhook-redelivery dedup: *"nếu chức năng sẵn có không có chống trùng lặp, thì tính năng bổ sung này cũng không cần"* (if the existing feature set has no dedup mechanism, this add-on feature doesn't need one either). Verified via codebase search: no `webhook_event`/`webhook_delivery` table exists anywhere, and `deliveryId` is logged only, never persisted — so there is no existing precedent for redelivery dedup on any webhook consumer in this platform. The pre-existing `REDELIVERY_DEDUPE_GAP` exception record in `self-review.md` §9 is updated from OPEN to human-ratified Accepted Risk. Terminology (`total_check_count`) and Business Rule #6 in `spec-pack.md` annotated to scope the "exactly once per PR" guarantee as covering distinct lifecycle events only, not transport-level redelivery of the same event | Ticket owner (Phase 5 Codex-review round 3) |
| 2026-08-17 | OI-16 | Resolved. The catch block around the shared, PR-level `resolveRevision`/`listTree` calls in `GithubWebhookService.validateTemplateUsage` changed from a blanket `catch (Exception ex) { log.warn(...); return; }` to a status-aware `catch (WebClientResponseException ex) { if (isSkippableFetchError(ex)) { log.warn(...); return; } throw ex; }`, reusing the existing `isSkippableFetchError` method already used for per-file blob fetches (per the ticket owner's decision to classify like per-file fetches). `GithubWebhookServiceTest` gained 2 new tests: one asserting 401/403/404 skip validation silently with `recordIndependently` never invoked, one asserting a 500 propagates and fails the request. Business Rule #10 and the §6.4 error table in `spec-pack.md` updated to cover this shared-call case explicitly | Codex automated review (Phase 5 round 3), decision confirmed by ticket owner |
| 2026-08-17 | OI-17 | Resolved. The `templateUsageStatWriter.recordIndependently(...)` call site in `GithubWebhookService.validateTemplateUsage`'s per-file loop is now wrapped in `try { ... } catch (DataAccessException ex) { log.warn(...); }`, so a counter-write failure for one file is logged and skipped rather than aborting the webhook. `GithubWebhookServiceTest` gained a new test using a 2-file PR where the first `recordIndependently(...)` call throws `QueryTimeoutException` and the second succeeds, asserting the webhook completes normally and both files are attempted. §6.4 error table in `spec-pack.md` updated with this failure mode | Codex automated review (Phase 5 round 3) |
