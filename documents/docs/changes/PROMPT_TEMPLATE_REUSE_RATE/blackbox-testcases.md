# Black-box Test Cases

**Ticket ID**: PROMPT_TEMPLATE_REUSE_RATE
**Create date**: 2026-08-17
**Author**: QA Designer (Phase 6 — Black-box Test Spec)
**Update date**: 2026-08-17

> Viết cho người không đọc code: mọi "Expected result" đều là điều một người dùng/QA có thể tự mắt xác nhận được trên PR GitHub, trên màn hình PM Dashboard, hoặc trong dữ liệu (số liệu hiển thị). Không tham chiếu tên hàm/API/DTO nội bộ.

## blackbox-testcases

| ID | AC | Viewpoint | Dữ liệu tiền đề | Thủ tục | Input | Expected result | Priority | Note |
|---|---|---|---|---|---|---|---|---|
| BB-001 | AC-1 | Normal | Có 1 PR đang mở, sửa đúng 1 file tài liệu nằm trong thư mục của ticket (`documents/docs/changes/{mã-ticket}/spec-pack.md`), nội dung tiêu đề khớp hoàn toàn với mẫu chuẩn | Merge PR | PR merge với file trên | Trên PM Dashboard, phase "Spec Pack" của đúng dự án/repository tăng thêm 1 lượt kiểm tra và 1 lượt đạt chuẩn | P0 | Kịch bản cơ bản nhất của tính năng |
| BB-002 | AC-1 | Normal | PR sửa 1 file nằm ngoài thư mục ticket (vd. mã nguồn ứng dụng, không thuộc `documents/docs/changes/...`) | Merge PR | PR merge với file ngoài phạm vi | Không có phase nào trên Dashboard thay đổi số liệu vì file này | P1 | Xác nhận hệ thống không quét nhầm file không liên quan |
| BB-003 | AC-2 | Normal | File tài liệu có mẫu chuẩn tồn tại tại vị trí ưu tiên số 1 (thư mục mẫu gốc) | Merge PR sửa file đó, nội dung tiêu đề khớp mẫu tại vị trí 1 | PR merge | Được ghi nhận PASS đúng theo mẫu ở vị trí ưu tiên 1 | P0 | |
| BB-004 | AC-2 | Normal | Loại tài liệu chỉ có mẫu ở vị trí ưu tiên thứ 2 (thư mục "_ticket-template"), không có ở vị trí 1 | Merge PR sửa file loại tài liệu này | PR merge | Hệ thống vẫn tìm ra và dùng đúng mẫu ở vị trí 2 để so sánh, ghi nhận kết quả bình thường (không bị bỏ qua) | P1 | |
| BB-005 | AC-2 | Normal | Loại tài liệu có mẫu tồn tại ở CẢ vị trí 1 và vị trí 2, hai mẫu có cấu trúc tiêu đề khác nhau | Merge PR sửa file với tiêu đề khớp mẫu ở vị trí 1 nhưng KHÔNG khớp mẫu ở vị trí 2 | PR merge | Kết quả PASS (hệ thống phải ưu tiên dùng mẫu ở vị trí 1, không phải vị trí 2) | P1 | Kiểm tra đúng thứ tự ưu tiên, không chỉ là "tìm thấy mẫu nào đó" |
| BB-006 | AC-4 | Normal | File thay đổi có toàn bộ tiêu đề (tên phần, cấp độ, thứ tự) giống hệt mẫu, nhưng nội dung bên trong từng phần (đoạn văn, bảng, số liệu) khác hoàn toàn với mẫu | Merge PR | PR merge | Kết quả vẫn là PASS — thay đổi nội dung bên trong không làm rớt kết quả | P0 | Bảo vệ đúng nguyên tắc "chỉ so cấu trúc tiêu đề, không so nội dung" |
| BB-007 | AC-6 | Normal | Quản trị viên thêm cấu hình dữ liệu để một loại tài liệu mới (trước đây chưa được biết đến) được tính vào 1 phase đang có sẵn — chỉ thao tác trên dữ liệu cấu hình, không có bản cập nhật phần mềm nào được triển khai | Merge 1 PR có sửa loại tài liệu mới này | PR merge | Phase tương ứng trên Dashboard tự động ghi nhận thêm lượt kiểm tra cho file thuộc loại mới, không cần chờ release phần mềm | P1 | Xác nhận việc gắn loại tài liệu vào phase là cấu hình, không phải code cứng |
| BB-008 | AC-7 | Normal | Người dùng có quyền PM của một dự án, dự án đã có lịch sử kiểm tra tài liệu ở nhiều phase khác nhau | Đăng nhập, vào PM Dashboard, chọn đúng dự án + repository | Xem mục "Tỷ lệ tái sử dụng mẫu theo giai đoạn" | Hiển thị đủ tất cả 8 giai đoạn nghiệp vụ, mỗi dòng có tên giai đoạn, số lượt kiểm tra/số lượt đạt, và tỷ lệ % | P0 | Test hiển thị chính, phải xác nhận component thật sự lên màn hình (không chỉ unit test) |
| BB-009 | AC-3 | Abnormal | Có 1 loại tài liệu chưa từng có mẫu chuẩn ở bất kỳ vị trí nào trong 3 vị trí quy định | Merge PR sửa file loại tài liệu này (PR có thêm các file khác có mẫu hợp lệ) | PR merge | File không có mẫu bị bỏ qua hoàn toàn: không có thông báo lỗi nào hiển thị cho người tạo PR, số liệu Dashboard không đổi vì file này; các file khác trong cùng PR vẫn được tính bình thường | P0 | Bảo vệ nguyên tắc "im lặng bỏ qua", không được báo lỗi giả |
| BB-010 | AC-4 | Abnormal | File thay đổi có đúng số lượng và đúng tên các phần như mẫu, nhưng thứ tự các phần bị đảo ngược so với mẫu | Merge PR | PR merge | Kết quả là FAIL (không đạt), dù tên từng phần đều đúng | P1 | Xác nhận thứ tự tiêu đề cũng được tính, không chỉ tên/số lượng |
| BB-011 | 6.4 | Abnormal | Một request gửi tới webhook GitHub có chữ ký xác thực (bảo mật) không hợp lệ hoặc bị giả mạo | Gửi request | Request với chữ ký sai | Toàn bộ request bị từ chối ngay từ đầu; không có bước xử lý/ghi nhận nào diễn ra, kể cả các file hợp lệ trong đó | P0 | Bảo mật — không được xử lý một phần rồi mới từ chối |
| BB-012 | 6.4 | Abnormal (External IF failure) | PR có 2 file hợp lệ (có mẫu). Khi hệ thống lấy nội dung 1 trong 2 file từ GitHub thì bị từ chối truy cập/không tìm thấy (file/quyền không còn hợp lệ tại thời điểm đó) | Merge PR | PR merge, 1 trong 2 file gặp lỗi truy cập nội dung | File gặp lỗi bị bỏ qua (không cộng số liệu cho file đó), file còn lại vẫn được kiểm tra và ghi nhận bình thường, không có lỗi hiển thị cho người dùng | P0 | Ranh giới "lỗi 1 file không kéo sập cả PR" |
| BB-013 | 6.4 | Abnormal (External IF failure) | PR có 1 file hợp lệ. Khi lấy nội dung file đó từ GitHub, hệ thống GitHub gặp sự cố tạm thời (không phải do file/quyền, mà do lỗi hệ thống phía GitHub) | Merge PR | PR merge, GitHub trả lỗi hệ thống khi tải nội dung | Toàn bộ việc xử lý webhook cho PR này thất bại thật sự (không bị âm thầm bỏ qua như trường hợp BB-012) | P1 | Phân biệt "lỗi do không tìm thấy/không có quyền" và "lỗi hệ thống thật" |
| BB-014 | 6.4 | Abnormal (External IF failure) | Ngay bước đầu tiên xác định nội dung tổng thể của PR (trước khi xét từng file), GitHub từ chối truy cập/không tìm thấy dữ liệu PR | Merge PR | PR merge, bước xác định PR thất bại vì không có quyền/không tìm thấy | Toàn bộ việc kiểm tra mẫu cho PR này bị bỏ qua hoàn toàn (không file nào trong PR được tính), không có lỗi hiển thị cho người dùng, các phần xử lý PR khác (không liên quan tính năng này) không bị ảnh hưởng | P1 | |
| BB-015 | 6.4 | Abnormal (External IF failure) | Ngay bước đầu tiên xác định nội dung tổng thể của PR, GitHub gặp sự cố hệ thống thật (không phải do quyền/không tìm thấy) | Merge PR | PR merge, bước xác định PR gặp lỗi hệ thống GitHub | Toàn bộ webhook xử lý PR này thất bại thật sự | P1 | |
| BB-016 | 6.4 | Abnormal (External IF failure — nội bộ) | PR có 2 file hợp lệ. Khi ghi số liệu vào hệ thống cho file thứ nhất, cơ sở dữ liệu gặp sự cố tạm thời | Merge PR | PR merge | Số liệu của file thứ nhất không được ghi nhận, nhưng file thứ hai trong cùng PR vẫn được ghi nhận bình thường; toàn bộ webhook vẫn hoàn tất, không báo lỗi cho người dùng | P0 | Bảo vệ nguyên tắc "lỗi ghi số liệu không được làm sập cả webhook" |
| BB-017 | 6.4 | Abnormal | File thay đổi thuộc một loại tài liệu mà hệ thống không xác định được nó thuộc giai đoạn (phase) nào | Merge PR | PR merge | File bị loại khỏi thống kê, không có lỗi phát sinh cho người dùng | P2 | |
| BB-018 | 6.5 | Boundary | Một giai đoạn (phase) của dự án/repository từ trước đến nay chưa từng có file nào được kiểm tra (0 lượt) | Vào PM Dashboard, chọn đúng dự án + repository | Xem dòng của phase đó | Dòng phase đó vẫn xuất hiện đầy đủ (không bị ẩn/xoá khỏi danh sách), số lượt kiểm tra và số lượt đạt đều là 0, tỷ lệ hiển thị là dấu "-" (không phải "0%") | P0 | Đúng ranh giới nghiệp vụ quan trọng nhất của tính năng — dễ bị phá vỡ nếu sửa sai truy vấn dữ liệu |
| BB-019 | 6.5 | Boundary | Một phase mà toàn bộ các lượt kiểm tra từ trước tới nay đều đạt PASS (số lượt đạt = tổng số lượt kiểm tra) | Vào PM Dashboard | Xem dòng của phase đó | Tỷ lệ hiển thị đúng 100% | P1 | |
| BB-020 | 6.5 | Boundary | File thay đổi không có bất kỳ tiêu đề nào (toàn bộ là văn bản thường), trong khi mẫu chuẩn có ít nhất 1 tiêu đề | Merge PR sửa file này | PR merge | Kết quả là FAIL | P1 | |
| BB-021 | 6.5 | Boundary | PR chỉ sửa các file nằm hoàn toàn ngoài mọi thư mục ticket (0 file hợp lệ để kiểm tra) | Merge PR | PR merge | Không có phase nào trên Dashboard thay đổi số liệu | P2 | Trùng viewpoint với BB-002, giữ lại vì là ranh giới "0 file" chính thức trong đặc tả |
| BB-022 | 6.5 | Boundary | Dữ liệu hệ thống có các giai đoạn/luồng xử lý khác không thuộc phạm vi tính năng này (ví dụ luồng rà soát an toàn AI dùng chung bảng phân loại tài liệu) | Vào PM Dashboard, chọn 1 dự án/repository có dữ liệu ở cả các luồng trong phạm vi và ngoài phạm vi | Xem danh sách các dòng phase hiển thị | Chỉ hiển thị đúng 8 giai đoạn nghiệp vụ quy định (Spec Pack, Khởi tạo tài liệu làm việc, Kế hoạch triển khai, Checklist Review, Triển khai & Review, Kế hoạch & Kết quả Test, Blackbox Test, Report); các luồng khác không xuất hiện dù có dữ liệu riêng | P0 | Ranh giới đã từng bị lỗi ở vòng review trước (rò rỉ phase ngoài phạm vi) — ưu tiên cao |
| BB-023 | AC-4 / Character type | Abnormal | File thay đổi có tiêu đề dùng chữ số toàn chiều rộng ("full-width", ví dụ "１. Tổng quan" — thường gặp khi gõ bằng bàn phím tiếng Nhật/Trung), trong khi mẫu chuẩn dùng số bình thường ("1. Tổng quan") | Merge PR | PR merge | Kết quả là FAIL vì tên tiêu đề bị coi là khác nhau (số toàn chiều rộng khác số bình thường) | P2 | Nhắc QA để ý dữ liệu nhập từ bàn phím CJK khi soạn tài liệu |
| BB-024 | AC-4 / Character type | Abnormal | File thay đổi có tiêu đề khác mẫu chỉ ở cách viết hoa/thường 1 chữ cái (vd. "Tổng Quan" so với "Tổng quan") | Merge PR | PR merge | Kết quả là FAIL (được tính là khác tên) | P2 | Theo đúng nguyên tắc "khác bất kỳ điểm nào ở tên là FAIL" |
| BB-025 | Business Rule #11 | State transition | Có 1 PR đang ở trạng thái "mới mở" (chưa đóng) | Không merge, chỉ mở PR (hoặc thêm commit mới vào PR đang mở) | Sự kiện "mở PR" / "cập nhật thêm commit" / "mở lại PR" | Không có phase nào trên Dashboard thay đổi số liệu ở bước này | P0 | Phải test cả 3 biến thể: mở mới, thêm commit, mở lại — số liệu chỉ được tính khi đóng+merge |
| BB-026 | Business Rule #11 | State transition | Có 1 PR đủ điều kiện kiểm tra, đang được đóng lại nhưng KHÔNG merge (đóng bỏ, huỷ PR) | Đóng PR mà không merge | Đóng PR (không merge) | Không có phase nào trên Dashboard thay đổi số liệu | P0 | Ranh giới dễ nhầm: "đóng" khác với "đóng và merge" |
| BB-027 | Business Rule #11 / AC-5 | State transition | Có 1 PR đủ điều kiện kiểm tra (có file hợp lệ, có mẫu, tiêu đề khớp) | Đóng PR VÀ merge | Đóng + merge PR | Số liệu của phase liên quan tăng đúng 1 lần (không tăng nhiều lần dù trước đó PR đã từng mở/cập nhật/đóng-mở lại nhiều lần) | P0 | Xác nhận việc tính điểm chỉ xảy ra đúng 1 lần tại thời điểm merge, bất kể lịch sử trạng thái trước đó của PR |
| BB-028 | OI-15 | Double submit | 1 PR đã đóng + merge và đã được tính số liệu 1 lần. Do đặc tính gửi sự kiện của GitHub, hệ thống nhận lại đúng sự kiện "đóng + merge" đó thêm 1 lần nữa cho cùng PR | Giả lập GitHub gửi lại (duplicate) đúng sự kiện đóng+merge đã xử lý trước đó | Gửi lại sự kiện trùng lặp | Số liệu của phase liên quan bị cộng thêm 1 lần nữa (tăng gấp đôi so với thực tế 1 PR) — đây là hành vi đã được xác nhận và CHẤP NHẬN là rủi ro đã biết, hệ thống hiện chưa có cơ chế chống trùng cho trường hợp này | P1 | KHÔNG phải bug cần fix trong ticket này — ghi nhận đúng hành vi thực tế đã được chốt (`OI-PROMPT_TEMPLATE_REUSE_RATE-15`, Accepted Risk). Nếu test cho ra kết quả KHÔNG bị cộng trùng, cần báo lại vì khác với quyết định đã ghi nhận |
| BB-029 | OI-9 | Permission | Người dùng A có vai trò PM được gán cho đúng dự án X | Đăng nhập bằng người dùng A, vào PM Dashboard, chọn dự án X | Xem mục thống kê tỷ lệ tái sử dụng mẫu | Xem được đầy đủ số liệu của dự án X | P0 | |
| BB-030 | OI-9 | Permission | Người dùng B đã đăng nhập hợp lệ nhưng KHÔNG có vai trò PM ở dự án X | Đăng nhập bằng người dùng B, thử xem thống kê của dự án X | Yêu cầu xem số liệu dự án X | Bị từ chối truy cập, không xem được số liệu của dự án X | P0 | Đảm bảo không rò rỉ dữ liệu dự án cho người không có quyền |
| BB-031 | OI-9 | Permission | Người dùng C có vai trò Quản trị hệ thống (ADMIN), không được gán riêng vai trò PM ở dự án X | Đăng nhập bằng người dùng C, xem thống kê của dự án X | Yêu cầu xem số liệu dự án X | Vẫn xem được đầy đủ số liệu (quyền ADMIN có hiệu lực trên mọi dự án) | P1 | |
| BB-032 | 6.2 Input | Abnormal (thiếu input bắt buộc) | Người dùng đã vào PM Dashboard nhưng chưa chọn đủ cả dự án và repository | Vào PM Dashboard, chỉ chọn 1 trong 2 (hoặc chưa chọn gì) | Chỉ chọn dự án, chưa chọn repository (và ngược lại) | Mục thống kê tỷ lệ tái sử dụng mẫu chưa hiển thị số liệu nào (không gọi lấy dữ liệu khi thiếu điều kiện lọc), không hiển thị lỗi gây khó hiểu cho người dùng | P2 | |
| BB-033 | Regression (đã fix ở vòng review trước) | State (loading) | Dự án/repository được chọn có dữ liệu thống kê hợp lệ, nhưng lần đầu dữ liệu đang trong quá trình tải về từ máy chủ | Vào PM Dashboard, quan sát ngay thời điểm vừa chọn xong dự án + repository | — | Trong lúc đang tải, màn hình KHÔNG được hiển thị nhầm thông báo "chưa có dữ liệu"/rỗng; chỉ sau khi tải xong mới hiển thị đúng số liệu thật hoặc thông báo rỗng thật sự nếu đúng là chưa có dữ liệu | P1 | Bug đã từng xảy ra và được sửa ở vòng review trước — giữ lại test này để chặn tái diễn |

## Viewpoints Covered

- [x] Normal case
- [x] Error case
- [x] Boundary value
- [x] Permission difference
- [x] State transition
- [x] Character type input
- [ ] Numeric input — không có input dạng số tự do do người dùng nhập trong phạm vi tính năng này (chỉ có `projectId`/`repositoryId` chọn từ danh sách có sẵn); không có ô nhập số liệu thủ công nào cần kiểm thử riêng
- [x] Full-width number
- [x] Empty/null (0 file hợp lệ, 0 lượt kiểm tra, chưa chọn đủ filter)
- [x] Duplicate (double submit — webhook redelivery)
- [ ] Non-existing ID — dự án/repository trong danh sách lọc luôn được chọn từ dữ liệu có sẵn trên màn hình (dropdown), không có luồng người dùng tự gõ ID để tạo ra trường hợp "ID không tồn tại"; nếu có luồng gọi trực tiếp bằng ID không hợp lệ, cần bổ sung sau khi xác nhận với đội phát triển
- [ ] Deleted data — không có khái niệm "xoá" trong phạm vi tính năng (chỉ có tài liệu/PR mới); không áp dụng
- [x] External IF failure
- [ ] Timeout/retry — có liên quan tới "lỗi hệ thống GitHub tạm thời" (BB-013/BB-015) nhưng cơ chế retry cụ thể không được mô tả trong đặc tả — nếu có cơ chế retry cần bổ sung riêng
- [x] Double submit
- [x] Back/reload (gộp trong BB-033, tải lại dữ liệu Dashboard)
- [ ] Session expired — không phải hành vi riêng của tính năng này, dùng chung cơ chế đăng nhập/hết phiên hiện có của toàn hệ thống, không thuộc phạm vi test riêng của ticket này
- [x] Existing data compatibility (BB-004/BB-005 mẫu cũ tồn tại song song)
- [x] Log/audit/notification/report output (BB-011/BB-012/BB-013/BB-014/BB-015/BB-016 — hành vi ghi log khi bỏ qua/lỗi)
