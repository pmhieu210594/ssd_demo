# Source Availability — PROMPT_TEMPLATE_REUSE_RATE

*Last updated: 2026-08-10 (revised after kickoff review — all PJ/HD resolved)*

## Solid Evidence

Evidence directly observed in source code, configuration, or migration files.

| Area | Evidence | Source |
|------|----------|--------|
| Webhook entry point | `pull_request` event handled by `GithubWebhookController` → `GithubWebhookService.handlePullRequest()`; HMAC verified before any DB write; route is `permitAll` | `EDCAP_BE/.../web/webhook/GithubWebhookController.java`, `application/usecase/ingestion/GithubWebhookService.java` |
| Changed file list | `GithubPullRequestFilesPort.listChangedFilePaths(repoKey, prNumber)` returns changed paths (no content) for the PR | `application/usecase/ingestion/GithubWebhookService.java` |
| Raw file content fetch | `ArtifactScannerSourcePort.resolveRevision()`, `.listTree(repositoryFullName, revisionSha)` (path → blob SHA), `.readBlob(repositoryFullName, blobSha)` (base64-decoded content) — already implemented against the GitHub Git Data API; no new external integration needed to read a changed file's or a template's raw markdown | `application/port/out/integration/ArtifactScannerSourcePort.java`, `infrastructure/github/GithubArtifactScannerSourceAdapter.java` |
| Markdown structural parser | `MarkdownParserCore.parse(content, sourcePath)` returns `MarkdownDocument.sections()` as `MarkdownSection(title, canonicalKey, level, body, startLine, endLine)` — captures header name, level (H1–H6), and order, matching exactly what the "compare header name/order/level only" rule needs | `domain/service/markdown/core/MarkdownParserCore.java` |
| Phase dimension table (existing) | `tbl_dim_phase` seeded with 10 rows: `0-A, 0-B, 1..9`, with `phase_code`, `phase_name`, `phase_order`. Rows `'1'..'8'` match the raw requirement's Phase 1–8 names and order exactly (`Spec Pack, Working Files Initialization, Implementation Plan, Review Checklist, Implementation and Review, Test Plan and Results, Blackbox Test, Report`) | `V4__init_shema_v2.sql` lines 1377–1389 |
| Artifact-type dimension table (existing) | `tbl_dim_artifact_type` (FK `phase_id` → `tbl_dim_phase`) already holds 13 rows: `SPEC_PACK, SOURCES, IMPL_PLAN, REVIEW_CHECKLIST, SELF_REVIEW, TEST_PLAN, TEST_RESULTS, BLACKBOX_TESTCASES, TEST_DATA, REPORT, CLAUDE_MD, CLAUDE_SETTINGS, CLAUDE_RULE` — a DB-driven, extensible phase→file mapping already satisfies the "no hard-coded phase/file logic" requirement | `V4__init_shema_v2.sql` lines 1391–1452, `V160__artifact_scanner.sql`, `V234__tbl_fact_exception_add_source_section.sql` |
| Generic metric-value fact table (existing) | `tbl_fact_metric_value` — generic counter/measurement table with `metric_code`, `period_type/period_start/period_end`, `value NUMERIC(18,4)`, `breakdown JSONB`, dimension FKs (`organization_id, project_id, repository_id, ticket_id, team_id`) — a candidate reuse target for `total_check_count`/`template_match_count` per phase | `V4__init_shema_v2.sql` lines 946–970 |
| Counter-shape fact table precedents | `tbl_fact_test_run` (`test_count/passed_count/failed_count/skipped_count`), `tbl_fact_evidence_quality_score` (fixed per-phase score columns) — precedent for a dedicated counter table if the generic metric table is judged unsuitable | `V4__init_shema_v2.sql` |
| Plain-JDBC read-adapter pattern | Aggregation/statistics reads use `NamedParameterJdbcTemplate` with CTEs and private row mappers, not MyBatis XML — reference pattern for the new stats endpoint | `infrastructure/persistence/adapter/DataOpsDashboardJdbcAdapter.java` |
| Layering convention | `web/rest/<Feature>Controller` → `application/usecase/<area>/<Feature>Service` → `application/port/out/persistence/<Feature>RepositoryPort` → `infrastructure/persistence/adapter/<Feature>JdbcAdapter` | Confirmed across `pmdashboard`, `dataops`, `ingestion` packages |
| Best-Effort Audit/Stat Write Pattern | Non-critical write-side effects (audit rows, stats) use `NESTED`/`REQUIRES_NEW` transaction propagation so a write failure does not roll back or block the primary webhook flow | `docs/architecture/service-layer-map.md`, confirmed against existing audit-write call sites |
| Flyway migration numbering | Latest migration is `V508__add_submitted_by_to_review.sql`; next migration for this ticket must be `V510__*.sql` | `EDCAP_BE/src/main/resources/db/migration/` directory listing |
| FE dashboard drawer | `TicketDetailDrawer.tsx` is composed of section sub-components (`SummaryBadges`, `PhaseCard`, `EqsSummaryCard`, `OpenIssuesCard`, ...) driven by `useQuery` under query-key prefix `["pm-dashboard", ...]`; natural anchor point for a new "Template Usage by Phase" section, closest existing pattern is `EqsSummaryCard` (per-phase score rows) | `EDCAP_FE/src/pages/pm-dashboard/components/TicketDetailDrawer.tsx` |
| FE API client convention | `lib/api.ts` exposes typed per-feature endpoint namespaces (`api.get<T>(path)`); new endpoint would be added as `templateUsage.statistics()` | `EDCAP_FE/src/lib/api.ts` |
| API URL/response convention | `/api/v1/<resource>` prefix; raw DTO/array success response (no envelope); `limit`/`offset` pagination (not applicable here — response is a small fixed-size array) | `docs/standards/api-contract.md` |
| Ticket-key path detection (existing, reusable) | `GithubWebhookService.ticketKeyFromPath(path)` finds the ticket key via `path.indexOf("/changes/")` (or `path.startsWith("changes/")`) and takes the next path segment — a **substring search, not a fixed literal prefix**. It already matches both `documents/changes/{ticket}/...` and `documents/docs/changes/{ticket}/...` without modification | `application/usecase/ingestion/GithubWebhookService.java` lines 434–449 |
| Skippable-blob-fetch pattern (existing, reusable) | `GithubSecurityEvidenceSnapshotService.readBlobAsBytes()`/`readBlobAsText()` catch `WebClientResponseException`, call `isSkippableBlobError(ex)` (`401`/`403`/`404` → log `warn` "Skipping missing GitHub blob..." and return `null`/`""`; any other status → rethrow, failing the caller) — the established precedent for "one file's fetch fails, don't fail the whole PR" | `application/usecase/ingestion/GithubSecurityEvidenceSnapshotService.java` lines 572–609 |
| Dedicated counter-table column shape (existing precedent) | `tbl_fact_test_run`: `UUID PK`, dimension FKs, `INT ... DEFAULT 0` counters, `CHECK (... >= 0)` non-negative constraints, `collected_at TIMESTAMPTZ NOT NULL DEFAULT now()` — the concrete precedent followed for the new `tbl_fact_template_usage_stat` table | `V4__init_shema_v2.sql` lines 676–698 |
| FE PMDashboardPage filter scope (existing) | `PMDashboardPage.tsx` already owns `filters.projectId`/`filters.repositoryId` (via `usePmDashboardFilters`) and gates `summaryQuery`/`ticketsQuery` on `Boolean(filters.projectId) && Boolean(filters.repositoryId)` — the exact scoping pattern to reuse for the new project/repository-scoped template-usage query | `EDCAP_FE/src/pages/pm-dashboard/PMDashboardPage.tsx` |
| FE placement anchor (confirmed) | `<AllTicketsTable ... />` renders at line 212, immediately followed by `<TicketDetailDrawer ... />` at line 227 — the new "Template Usage by Phase" section is inserted between these two | `EDCAP_FE/src/pages/pm-dashboard/PMDashboardPage.tsx` |
| PM Dashboard controller/service base (existing, reusable) | `PmDashboardController` is `@RequestMapping("/api/v1/pm/dashboard")` with existing `@GetMapping` handlers (`/summary`, `/insights`, `/tickets`, `/tickets/{ticketId}/detail`, `/access`, `/options`), each delegating to `PmDashboardService`; new endpoint added as a sibling `@GetMapping("/template-usage")` handler/service method — no new controller/service class | `web/rest/PmDashboardController.java`, `application/usecase/pmdashboard/PmDashboardService.java` |
| PM-role permission gate (existing, reusable) | `PmDashboardService.requirePm(AuthUserContext caller, UUID projectId)` (lines 197–212): ADMIN system role always passes; otherwise caller must hold the `PM` project role for the given `projectId` (`repository.findProjectRole(...)`), else throws `ForbiddenException("Component.Permission.Denied")`. Already called by every other PM Dashboard read method (`PmDashboardController.java:98` and `PmDashboardService.java:54,69,86,95,101,121`) — the exact gate to reuse for the new endpoint | `application/usecase/pmdashboard/PmDashboardService.java` lines 191–212 |

## Missing Evidence

Evidence not found in source, or found to contradict the raw requirement / architecture docs.

| Item | Looked in | Why missing / mismatch |
|------|-----------|-------------|
| `ConnectorOrchestrator` service | `application/usecase/collection/` (Glob + Grep across full BE source) | Referenced as an existing service in `docs/architecture/service-layer-map.md`, but no such class exists in source. Doc/source mismatch — likely aspirational or stale documentation; not directly relevant to this ticket but noted since the architecture doc was used as an input. |
| `tbl_dim_artifact_type` rows for `OPEN_ISSUES`, `CONTEXT`, `CODEX_REVIEW` | `V4__init_shema_v2.sql`, `V160__artifact_scanner.sql`, `V234__tbl_fact_exception_add_source_section.sql` (full grep of all `INSERT INTO tbl_dim_artifact_type`) | The raw requirement's Phase 1 (`open-issues.md`), Phase 2 (`context.md`), and Phase 5 (`codex-review.md`) files have no corresponding row in the existing dimension table. Only 13 of the ~16 files named across the 8 phases are currently seeded. |
| File-name match for Phase 3 | Raw requirement lists `impl_plan_template.md`; actual `tbl_dim_artifact_type.default_file_name` is `impl-plan.md`, and the actual template file on disk is `docs/standards/templates/_ticket-template/impl-plan.md` | Raw requirement text appears to reference the template's *source* name incorrectly; source/templates disagree with the raw requirement here. |
| Validation-scope path convention | Raw requirement says `documents/changes/{ticket-id}/...`; actual repository path (confirmed via this whole investigation and `project-edcap-repo-location` memory) is `documents/docs/changes/{ticket-id}/...` | The raw requirement's path is missing the `docs/` segment present in the real repo layout. Path-matching logic must use the real convention, not the literal string in the raw requirement. |
| API version prefix | Raw requirement example: `GET /api/template-usage/statistics`; `docs/standards/api-contract.md` mandates `/api/v1/<resource>` for all endpoints | Raw requirement omits the `v1` segment used by every other endpoint in the codebase. |
| FE scope of statistics (global vs per-ticket) | Raw requirement's JSON example has no ticket/project/repo filter (implies global); `TicketDetailDrawer.tsx` is strictly per-ticket (`ticketId` prop) | No existing code shows how phase-usage stats would be filtered per ticket; this is a real design gap, not just missing evidence. |
| FE zero-guard percentage formatter | `pages/pm-dashboard/utils.ts` (`formatScore`), `utils/number-format.ts` | Neither is a percentage formatter with a `total === 0 → "-"` guard; a new helper is needed. |
| Handling for "file read from GitHub fails mid-webhook" | `GithubWebhookService`, `ArtifactScannerSourcePort` call sites | No existing precedent found for a partial/soft failure of a single file's content fetch inside the webhook flow being handled without failing the whole request — needs an explicit decision (see PJ5/HD in this ticket). |

## Pending Judgements (PJ)

All resolved at kickoff review — see Resolved Decisions below.

## Human Decisions Required (HD)

All resolved at kickoff review — see Resolved Decisions below.

## Resolved Decisions

| ID | Decision | Date | Who |
|----|----------|------|-----|
| PJ1 / HD2 | New dedicated table `tbl_fact_template_usage_stat` (Option B), following the `tbl_fact_test_run` counter-table precedent, with columns `project_id, repository_id, phase_id, total_check_count, template_match_count` plus standard audit columns (id, created_at, updated_at) | 2026-08-10 | Stakeholder (via chat) |
| PJ2 / HD7 | Template file content is read from the **same PR head revision** as the changed file (Option A) | 2026-08-10 | Stakeholder (via chat) |
| PJ3 / HD3 | The 3 missing `tbl_dim_artifact_type` rows (`OPEN_ISSUES`, `CONTEXT`, `CODEX_REVIEW`) are added **in scope**, via the same `V510` migration (Option A) | 2026-08-10 | Stakeholder (via chat) |
| PJ4 / HD1 | Statistics are scoped by **project + repository** (not global, not per-ticket) — matches `PMDashboardPage`'s existing `filters.projectId`/`filters.repositoryId` scoping, and matches the new display location | 2026-08-10 | Stakeholder (via chat) |
| PJ5 / HD8 | On a single file's GitHub blob/tree fetch failure, reuse the existing `isSkippableBlobError` pattern from `GithubSecurityEvidenceSnapshotService`: `401/403/404` → skip that file (log `warn`, no counter change, continue processing); any other error → propagate/fail as today | 2026-08-10 | Stakeholder (via chat) |
| — (FE display location) | Display moves from `TicketDetailDrawer.tsx` to `PMDashboardPage.tsx`, rendered immediately after `<AllTicketsTable />` and before `<TicketDetailDrawer />` | 2026-08-10 | Stakeholder (via chat) |
| HD4 | Confirmed: no separate path-convention decision is needed. `ticketKeyFromPath()` already does a `/changes/` substring search (not a fixed literal prefix), so it transparently matches the real repo layout `documents/docs/changes/{ticket-id}/...`; the new file-under-ticket-scope check for template validation reuses this same approach | 2026-08-10 | Stakeholder (via chat), evidence at `GithubWebhookService.java:434-449` |
| HD5 | API URL confirmed as `GET /api/v1/pm/dashboard/template-usage`, added to the existing `PmDashboardController`/`PmDashboardService` (no new controller/service class) — not the raw requirement's literal `/api/template-usage/statistics`, and not a standalone `/api/v1/template-usage/statistics` resource | 2026-08-10 | Stakeholder (via chat) |
| HD6 | `ConnectorOrchestrator` doc/source mismatch: accepted as out of scope for this ticket, no action taken | 2026-08-10 | Stakeholder (via chat) |
