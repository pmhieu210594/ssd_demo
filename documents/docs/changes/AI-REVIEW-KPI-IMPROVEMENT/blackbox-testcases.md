# blackbox-testcases

**Ticket ID**: AI-REVIEW-KPI-IMPROVEMENT
**Create date**: 2026-08-20
**Author**: QA Designer (Claude)
**Update date**: 2026-08-20

> Chuyển 12 Acceptance Criteria (AC-AIRKI-1..12) trong `spec-pack.md` §6 thành test case dạng
> black-box: viết theo màn hình, thao tác nghiệp vụ (merge PR trên GitHub, mở màn hình PM Dashboard)
> và kết quả người dùng nhìn thấy được — không dùng thuật ngữ API/DTO nội bộ. Dữ liệu mẫu cụ thể xem
> `test-data.md`.
>
> **Lưu ý về sai lệch tài liệu nguồn**: một số dòng Expected result dưới đây phản ánh hành vi
> **thực tế đã được xác nhận** (qua `open-issues.md`/`review-checklist.md`), khác với mô tả cũ trong
> `spec-pack.md` — các chỗ này được đánh dấu rõ trong cột Note, không âm thầm chép lại text đã lỗi thời.

## blackbox-testcases

| ID | AC | Viewpoint | Dữ liệu tiền đề | Thủ tục | Input | Expected result | Priority | Note |
|---|---|---|---|---|---|---|---|---|
| TC-AIRKI-01 | AC-1 | Normal | Ticket T1 (project/repo test, xem `test-data.md`) chưa từng có PR nào merge trước đó | 1. Tạo PR chứa file `docs/changes/T1/ai-review.md` đúng theo mẫu chuẩn (mục §8 đủ 7 dòng, số liệu hợp lệ). 2. Merge PR trên GitHub. 3. Mở PM Dashboard, chọn đúng project + repository của T1. | `ai-review.md` §8 với: Blocker/Major 4/5 (80%), Adoption 9/10 (90%), Valid 9/10 (90%), False Positive 1/10 (10%), Resolution 8/10 (80%) — xem bảng mẫu "Normal — mẫu chuẩn" trong test-data.md | Khu vực KPI mới trên Dashboard hiển thị đúng 5 số: 80.0%, 90.0%, 90.0%, 10.0%, 80.0% (tương ứng đúng tên chỉ số) | Cao | Test case gốc/tham chiếu cho các case khác |
| TC-AIRKI-02 | AC-2 | Loại ký tự (đa ngôn ngữ) | Ticket T2, tài liệu `ai-review.md` viết heading mục 8 bằng tiếng Anh ("## 8. Statistics") thay vì tiếng Việt | 1. Merge PR chứa `ai-review.md` của T2. 2. Mở Dashboard xem T2. | Heading tiếng Anh, nội dung bảng số liệu giống mẫu chuẩn | Dữ liệu vẫn được ghi nhận và hiển thị đúng như TC-AIRKI-01, không bị bỏ qua vì tiêu đề khác ngôn ngữ | Cao | Hệ thống định vị theo cấu trúc/thứ tự mục, không theo chữ tiêu đề |
| TC-AIRKI-03 | AC-2 | Loại ký tự (đa ngôn ngữ, full-width) | Ticket T3, `ai-review.md` viết heading mục 8 bằng tiếng Nhật ("## 8. 統計データ", chứa ký tự full-width) | 1. Merge PR chứa `ai-review.md` của T3. 2. Mở Dashboard xem T3. | Heading tiếng Nhật, nội dung bảng số liệu giống mẫu chuẩn | Dữ liệu vẫn được ghi nhận và hiển thị đúng như TC-AIRKI-01 | Trung bình | Bổ sung ngôn ngữ thứ 3 để chắc chắn không có hard-code theo text tiếng Anh/Việt |
| TC-AIRKI-04 | AC-3 | Normal (đổi cách diễn đạt) | Ticket T4, `ai-review.md` là file thật lấy từ 1 ticket đã hoàn thành trước đó (không phải dữ liệu tự tạo), có các dòng nhãn viết khác cách hành văn so với mẫu chuẩn nhưng đúng thứ tự 7 dòng | 1. Merge PR chứa `ai-review.md` (bản thật) của T4. 2. Mở Dashboard xem T4. | File `ai-review.md` thật (xem "Fixture thật" trong test-data.md) | Cả 5 số liệu được đọc đúng theo **đúng vị trí dòng**, không bị lệch dù nhãn diễn đạt khác chữ so với mẫu chuẩn | Cao | Bảo vệ nguyên tắc "map theo vị trí, không theo text nhãn" |
| TC-AIRKI-05 | AC-4 | Abnormal | Ticket T5, dòng "Tỷ lệ AI Review Adoption" trong `ai-review.md` ghi giá trị chữ "Không áp dụng" thay vì số | 1. Merge PR chứa `ai-review.md` của T5. 2. Mở Dashboard xem T5. | Dòng Adoption = "Không áp dụng"; 4 dòng còn lại hợp lệ | PR merge thành công bình thường (không có lỗi merge). Trên Dashboard: các chỉ số dùng chung mẫu số với Adoption (Adoption/Valid/False Positive/Resolution) đều hiển thị "-"; riêng Blocker/Major (mẫu số độc lập) vẫn hiển thị số bình thường | Cao | Không được hiện 0% hay báo lỗi cho ô "Không áp dụng" |
| TC-AIRKI-06 | AC-4 | Loại ký tự / số full-width | Ticket T6, dòng Blocker/Major ghi số dạng full-width Unicode: "５／１０" thay vì "5/10" | 1. Merge PR chứa `ai-review.md` của T6. 2. Mở Dashboard xem T6. | Dòng Blocker/Major = "５／１０" (full-width); các dòng khác hợp lệ | Chỉ số Blocker/Major Resolution Rate hiển thị "-" (không tính thành 0%, không tính nhầm thành 5/10); 4 chỉ số còn lại (mẫu số độc lập với Blocker) vẫn hiển thị đúng số | Trung bình | Số full-width không được hệ thống nhận diện như số thường |
| TC-AIRKI-07 | AC-4 | Boundary / loại ký tự | Ticket T7, dòng Adoption ghi số trộn nửa-full-width: "1２/15" (ký tự "２" là full-width) | 1. Merge PR chứa `ai-review.md` của T7. 2. Mở Dashboard xem T7. | Dòng Adoption = "1２/15" (trộn ký tự) | Cả 4 chỉ số dùng chung mẫu số với Adoption (Adoption/Valid/False Positive/Resolution) đều hiển thị "-" — **không** phải chỉ riêng Adoption bị ảnh hưởng; Blocker/Major vẫn hiển thị bình thường | Trung bình | Kiểm chứng quy tắc "1 dòng lỗi định dạng số → cả nhóm dùng chung mẫu số bị loại", không phải lỗi cục bộ từng dòng |
| TC-AIRKI-08 | AC-5 | State transition | Ticket T8 đã có dữ liệu KPI từ 1 lần merge PR trước (Blocker 80%, Adoption 90%,...) | 1. Tạo PR mới cho T8, cập nhật `ai-review.md` với số liệu mới khác lần trước (ví dụ Blocker 100%, Adoption 50%). 2. Merge PR mới này. 3. Mở lại Dashboard xem T8. | `ai-review.md` phiên bản 2 của T8, số liệu khác phiên bản 1 | Dashboard hiển thị **số liệu mới nhất** (100%, 50%,...), không còn số liệu cũ, không cộng dồn 2 lần merge thành số vượt quá 100% | Cao | Mỗi ticket chỉ có đúng 1 bộ số liệu hiện hành, luôn là lần merge gần nhất |
| TC-AIRKI-09 | AC-6 | Double submit | Ticket T9 vừa merge PR xong (đã ghi nhận số liệu lần 1) | 1. Trên GitHub, vào lịch sử gửi thông báo (Webhook deliveries) của PR vừa merge. 2. Bấm "Redeliver" để gửi lại đúng thông báo đó lần thứ 2. 3. Mở lại Dashboard xem T9. | Gửi lại đúng 1 thông báo merge cũ (không đổi nội dung `ai-review.md`) | Số liệu trên Dashboard **giữ nguyên y hệt** như sau lần merge đầu, không tăng gấp đôi, không tạo thêm dòng dữ liệu nào cho T9 | Cao | Idempotent khi GitHub gửi lại đúng 1 sự kiện (giả lập lỗi mạng phía GitHub) |
| TC-AIRKI-10 | AC-7 | External IF failure / Abnormal | Ticket T10, môi trường test được đội kỹ thuật giả lập tình trạng hệ thống tạm thời không ghi được dữ liệu thống kê (ví dụ CSDL thống kê tạm ngắt kết nối) | 1. Trong lúc môi trường đang ở trạng thái giả lập lỗi, merge PR chứa `ai-review.md` hợp lệ của T10. 2. Kiểm tra PR trên GitHub. 3. Khôi phục môi trường bình thường, mở Dashboard xem T10. | `ai-review.md` hợp lệ của T10, xảy ra trong lúc môi trường lỗi ghi thống kê | PR vẫn merge thành công bình thường trên GitHub (không có lỗi hiển thị cho người dùng GitHub); các xử lý khác của PR (ví dụ kiểm tra sử dụng template) vẫn hoàn tất. Dashboard của T10 **chưa** có số liệu KPI (do lần ghi đó thất bại) — số liệu sẽ xuất hiện đúng ở lần merge PR kế tiếp của T10 | Trung bình | Cần môi trường test có khả năng giả lập lỗi ghi CSDL, phối hợp với đội kỹ thuật để dựng kịch bản này |
| TC-AIRKI-11 | AC-8 | External IF failure | Ticket T11, PR merge có tham chiếu tới file `ai-review.md` nhưng file này không truy cập được từ GitHub (đã bị xoá quyền đọc/xoá file/token hết hạn — tương đương lỗi 401/403/404) | 1. Chuẩn bị PR của T11 sao cho việc lấy nội dung `ai-review.md` từ GitHub thất bại (phối hợp đội kỹ thuật). 2. Merge PR. 3. Kiểm tra PR trên GitHub, mở Dashboard xem T11 và các ticket khác cùng PR (nếu có). | PR merge trong tình trạng không lấy được nội dung `ai-review.md` của T11 | PR vẫn merge thành công, không có lỗi hiển thị trên GitHub; T11 không có số liệu KPI mới (bị bỏ qua); các ticket khác trong cùng PR (nếu có) vẫn được xử lý bình thường | Trung bình | Ticket lỗi bị bỏ qua độc lập, không làm hỏng toàn bộ luồng xử lý PR |
| TC-AIRKI-12 | AC-9 | Normal (tổng hợp) | Project/repository test có nhiều ticket (T1, T4, T8...) đã có dữ liệu KPI khác nhau | 1. Mở PM Dashboard. 2. Chọn đúng project + repository chứa các ticket trên. | Chọn project + repository đã có ≥ 2 ticket có dữ liệu | Khu vực KPI hiển thị 5 số liệu **tổng hợp toàn repository** (gộp theo đúng công thức tổng tử số/tổng mẫu số của tất cả ticket trong phạm vi, không phải trung bình cộng riêng lẻ từng ticket) | Cao | Số liệu là gộp theo cấp repository, không phải theo từng ticket |
| TC-AIRKI-13 | AC-9 | Boundary | Project/repository test hoàn toàn mới, chưa từng có ticket nào merge PR có `ai-review.md` | 1. Mở PM Dashboard. 2. Chọn project + repository chưa có dữ liệu nào. | Chọn project + repository rỗng dữ liệu | Cả 5 chỉ số đều hiển thị "-"; màn hình không báo lỗi, không để trắng/vỡ giao diện | Cao | Trường hợp mẫu số tổng = 0 trên toàn phạm vi |
| TC-AIRKI-14 | AC-9 | Boundary | Trong repository test, chỉ có ticket có dữ liệu Blocker/Major hợp lệ, còn lại toàn bộ ticket đều ghi "Không áp dụng" cho Adoption/Valid/False Positive/Resolution | 1. Mở Dashboard, chọn đúng project + repository này. | Repository có dữ liệu lệch nhóm như trên | Blocker/Major Resolution Rate hiển thị số tính được bình thường; 4 chỉ số còn lại hiển thị "-" — 2 nhóm mẫu số độc lập với nhau ở cấp tổng hợp | Trung bình | Kiểm chứng tính độc lập giữa 2 nhóm ở cấp tổng hợp repository, không chỉ ở cấp 1 ticket |
| TC-AIRKI-15 | AC-9 | Boundary (làm tròn số) | Repository test có tổng tử số/mẫu số ra tỷ lệ không phải số tròn, ví dụ tổng 1/3 | 1. Mở Dashboard, chọn project + repository có tỷ lệ tổng hợp = 1/3. | Dữ liệu tổng hợp cho tỷ lệ 33.33...% | Hiển thị đúng "33.3%" (làm tròn 1 chữ số thập phân), không phải "33%" hay "33.33%" | Thấp | Kiểm tra quy tắc làm tròn hiển thị |
| TC-AIRKI-16 | AC-10 | Permission (abnormal) | Tài khoản test "user-no-pm": không có vai trò PM trong project test, không phải ADMIN hệ thống | 1. Đăng nhập bằng "user-no-pm". 2. Cố mở/xem khu vực KPI của project test (qua Dashboard hoặc truy cập trực tiếp địa chỉ chứa dữ liệu). | Tài khoản không có quyền PM/ADMIN với project đang xem | Hệ thống từ chối, hiển thị thông báo không có quyền truy cập; không hiển thị bất kỳ số liệu KPI nào của project đó | Cao | Đúng theo AC-10 |
| TC-AIRKI-17 | AC-10 | Permission (normal) | Tài khoản test "user-admin": vai trò ADMIN hệ thống, không được gán PM riêng cho project test | 1. Đăng nhập bằng "user-admin". 2. Mở Dashboard, chọn project test. | Tài khoản ADMIN hệ thống | Xem được đầy đủ số liệu KPI bình thường như TC-AIRKI-12/13 | Cao | ADMIN luôn được phép, không cần gán PM riêng |
| TC-AIRKI-18 | AC-10 | Permission (normal, boundary vai trò) | Tài khoản test "user-pm-scoped": vai trò hệ thống là "Viewer" (không phải ADMIN) nhưng được gán vai trò PM riêng cho đúng project test | 1. Đăng nhập bằng "user-pm-scoped". 2. Mở Dashboard, chọn project test. | Tài khoản có PM đúng phạm vi project, vai trò hệ thống khác ADMIN | Vẫn xem được đầy đủ số liệu KPI bình thường, không bị chặn | Trung bình | Phân quyền theo vai trò PM gán riêng cho project, không theo vai trò hệ thống chung |
| TC-AIRKI-19 | AC-10 | Permission (abnormal, boundary phạm vi) | Tài khoản test "user-pm-other-project": có vai trò PM nhưng chỉ ở 1 project khác, không phải project test đang xem | 1. Đăng nhập bằng "user-pm-other-project". 2. Cố mở Dashboard, chọn project test (không phải project mà tài khoản này là PM). | Tài khoản PM sai phạm vi project | Bị từ chối truy cập giống TC-AIRKI-16, dù tài khoản có vai trò PM (nhưng sai project) | Trung bình | PM phải đúng đúng project đang xem, không áp dụng chéo project |
| TC-AIRKI-20 | (Hành vi hệ thống chung, liên quan AC-9) | Abnormal | Không áp dụng riêng ticket | Dùng công cụ test gọi trực tiếp vào địa chỉ dữ liệu KPI (không qua giao diện chọn project/repository) mà **thiếu** một trong hai điều kiện bắt buộc (thiếu project hoặc thiếu repository) | Request thiếu project hoặc thiếu repository | Hệ thống trả về lỗi hệ thống chung (không phải thông báo nhắc "vui lòng chọn đủ project/repository"); đây là hành vi đã xác nhận nhất quán trên toàn hệ thống, không phải lỗi riêng của tính năng KPI | Thấp | **Sai lệch tài liệu**: `spec-pack.md` §10 mô tả cũ là lỗi 400 kèm thông báo rõ ràng; hành vi thực tế đã xác nhận (OI-AIRKI-9) là lỗi hệ thống chung — test theo hành vi thực tế, không theo mô tả cũ |
| TC-AIRKI-21 | AC-11 | Normal | Project/repository test đã có dữ liệu KPI (như TC-AIRKI-12) | 1. Mở PM Dashboard. 2. Chọn project + repository có dữ liệu. 3. Quan sát khu vực số liệu mới. | — | Màn hình hiển thị 1 khu vực riêng dạng **thẻ/ô** (mỗi ô là 1 chỉ số kèm % giá trị), liệt kê đủ 5 chỉ số theo đúng 5 tên nghiệp vụ (Blocker/Major Resolution Rate, AI Review Adoption Rate, AI Review Valid Finding Rate, AI False Positive Rate, AI Finding Resolution Rate) | Cao | **Sai lệch tài liệu**: `spec-pack.md` §6/§9 mô tả cũ là hiển thị dạng bảng (CDataTable); quyết định thực tế đã chốt là dạng thẻ/ô giống khu vực tổng quan hiện có trên Dashboard — test theo giao diện thực tế |
| TC-AIRKI-22 | AC-11 | Boundary | Project/repository test có 1 vài chỉ số là "-" (như TC-AIRKI-05) | 1. Mở Dashboard, chọn project + repository có chỉ số null. | — | Ô chỉ số null hiển thị đúng ký hiệu "-", không hiển thị "0%", không để trống hoàn toàn, không có lỗi giao diện | Trung bình | |
| TC-AIRKI-23 | AC-12 | Structural/AC closure | Bất kỳ project/repository test nào | 1. Đăng nhập, vào PM Dashboard qua đường điều hướng thông thường (menu chính, không qua đường dẫn tắt/demo riêng). 2. Chọn project + repository. | — | Khu vực KPI mới xuất hiện đúng trên màn hình PM Dashboard chính, ở đúng vị trí đã thiết kế (ngay sau/gần khu vực tổng quan hiện có), không phải màn hình rời/tách biệt | Trung bình | Xác nhận tính năng thực sự được gắn vào màn hình thật, không chỉ tồn tại dưới dạng component cô lập |
| TC-AIRKI-24 | (Hành vi hỗ trợ AC-9, gating điều kiện lọc) | Normal / State transition | Người dùng vừa mở PM Dashboard, chưa chọn xong bộ lọc | 1. Mở PM Dashboard. 2. Chỉ chọn project, **chưa chọn** repository. 3. Quan sát khu vực KPI. 4. Chọn tiếp repository. 5. Quan sát lại. | Bước 2: chỉ có project. Bước 4: có đủ project + repository | Ở bước 3: khu vực KPI chưa hiển thị số liệu/chưa cố tải dữ liệu, không báo lỗi. Ở bước 5: khu vực KPI hiển thị số liệu đầy đủ ngay sau khi chọn đủ cả hai điều kiện | Thấp | Tránh gọi tải dữ liệu khi bộ lọc chưa đủ điều kiện |
