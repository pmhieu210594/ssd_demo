# Open Issues

**Ticket ID**: AI-REVIEW-KPI-IMPROVEMENT
**Create date**: 2026-08-18 | **Update date**: 2026-08-20
**Author**: SDD Analyst (Claude)

ID prefix: `OI-AIRKI` (Open Issue), `H-AIRKI` (Human Decision). Xem chi tiết kỹ thuật liên quan tại `spec-pack.md`.

| ID | Issue | Phase | Impact | Severity | Owner | Due date | Status |
|---|---|---|---|---|---|---|---|
| OI-AIRKI-1 | Tên KPI business yêu cầu "Blocker/Major Resolution Rate" nhưng label dòng tương ứng trong `_ticket-template/ai-review.md` chỉ ghi "(Blocker)", không đề cập "Major" — chưa rõ có nên gộp cả finding mức Major vào numerator/denominator này hay chỉ tính Blocker | Spec | DB schema (tên cột, logic đếm), độ chính xác KPI ngay từ bản đầu tiên | High | BA / Ticket owner | Trước khi bắt đầu impl-plan | **Resolved** (2026-08-18) |
| OI-AIRKI-2 | Shape API `ai-finding-stats`: `repositoryId` bắt buộc (1 repository/lần gọi, giống `template-usage`) hay optional (trả về list nhiều repository trong 1 project)? | Spec / Contract | FE/BE contract, cấu trúc query trong `PmDashboardJdbcAdapter` | High | BE lead / FE lead | Trước khi bắt đầu impl-plan | **Resolved** (2026-08-18) |
| OI-AIRKI-3 | Có cần hỗ trợ filter theo `periodKey` (sprint/tháng) cho tính năng này ở phiên bản đầu, giống các endpoint PM Dashboard khác (`summary`/`insights`/`tickets`)? | Spec | Có thể cần thêm cột thời gian vào bảng fact nếu chốt muộn | Medium | BA | Trước khi viết migration | **Resolved** (2026-08-18) |
| OI-AIRKI-4 | Chọn DB schema pattern cho `tbl_fact_ai_finding_stat`: tối giản (kiểu `V510__add_template_usage_tracking.sql`, chỉ `created_at`/`updated_at`) hay đầy đủ audit/soft-delete (kiểu `V509__ticket_bug_metrics.sql`)? | DB | Migration, khả năng truy vết lịch sử thay đổi | Medium | DB owner | Trước khi viết migration | **Resolved** (2026-08-18) |
| OI-AIRKI-5 | Vị trí hook mới trong `GithubWebhookService.handlePullRequest` (đặt SAU đoạn build `ticketScopes`, dòng ~293-296, khác vị trí `validateTemplateUsage` ở dòng ~253-256) cần review kỹ để đảm bảo không có tác dụng phụ với thứ tự các bước xử lý khác trong cùng hàm | Impl | Đúng đắn luồng xử lý webhook, rủi ro side-effect khi đổi thứ tự tương đối | Medium | BE reviewer | Trong code review của impl-plan | **Resolved** (2026-08-18) |
| OI-AIRKI-6 | Nội dung 33 test case hiện có trong `GithubWebhookServiceTest.java`, cùng các test PM Dashboard hiện có (`PmDashboardServiceTest`, `PmDashboardJdbcAdapterFindTemplateUsageTest`, `PmDashboardControllerTest`) chưa được đọc chi tiết | Test | Rủi ro trùng lặp/phá vỡ mock pattern hiện có khi viết test mới | Low | BE dev thực hiện impl | Trước khi viết test mới | **Resolved** (2026-08-18) — quyết định thủ tục, thực thi ở impl-plan |
| OI-AIRKI-7 | Xác nhận hành vi hiển thị khi rate = `null` trên FE: hiển thị `-` (theo đề xuất, đồng nhất với `template-usage`) chứ không phải `0%` hoặc ẩn hẳn dòng/cột | Spec / FE | UX nhất quán trên PM Dashboard | Low | UX / BA | Trước khi impl FE | **Resolved** (2026-08-18) |
| OI-AIRKI-8 | Ticket đã merge PR **trước khi** tính năng này được deploy sẽ không có dữ liệu trong `tbl_fact_ai_finding_stat` cho tới khi có merge tiếp theo — có cần chạy 1 lần backfill quét lại các ticket hiện có không? | Ops / Scope | Độ đầy đủ dữ liệu ngay sau khi go-live | Medium | PM / Ops | Trước khi go-live | **Resolved** (2026-08-18) |
| OI-AIRKI-9 | `GlobalExceptionHandler` không có `@ExceptionHandler` cho `MissingServletRequestParameterException` — thiếu `repositoryId`/`projectId` bắt buộc trả HTTP 500 `INTERNAL_ERROR`, không phải 400 như `spec-pack.md` §10 ngầm định. Defect có sẵn (cùng tồn tại ở `template-usage`), không do ticket này tạo ra, nhưng endpoint mới kế thừa hành vi này. Quyết định: chấp nhận 500 hiện tại, hay mở rộng phạm vi ticket thêm handler 400 dùng chung cho cả 2 endpoint? | Impl / Contract | Hành vi lỗi thực tế của endpoint mới, khả năng phải sửa `GlobalExceptionHandler` (file dùng chung) | High | BE lead | Trước khi code phần Error handling | **Resolved** (2026-08-19) |
| OI-AIRKI-10 | `A-AIRKI-3` (cột `ai_review_finding_total_count` là mẫu số dùng chung cho 4/5 KPI: adoption/valid/false-positive/resolution rate, trong khi `blocker_major_total_count` có mẫu số riêng) tự nhận trong `spec-pack.md` §16 là suy đoán AI, **chưa được hỏi trực tiếp người dùng** — khác các Human Decision H-AIRKI-1..8 đã xác nhận rõ ràng. Cần re-confirm vì ảnh hưởng trực tiếp số lượng cột DDL `V511`. | Spec / DB | DDL bảng `tbl_fact_ai_finding_stat`, đúng đắn công thức tính rate | High | BA / DB owner | Trước khi viết migration `V511` thật | **Resolved** (2026-08-19) |
| OI-AIRKI-11 | Chưa xác nhận `ArchitectureTest.java` (ArchUnit) có thực sự tồn tại trong `EDCAP_BE` hay không — glob `**/ArchitectureTest.java` và `**/*ArchitectureTest*` đều không ra kết quả trong 2 phiên nghiên cứu, trái với khẳng định "Confirmed" của `docs/standards/testing.md`. Nếu không tồn tại, code mới sẽ không có safety net ArchUnit tự động cho vi phạm layer boundary. | Test / Standards | Độ tin cậy của safety net kiến trúc cho code mới | Low | BE lead | Trước khi impl (xác nhận qua cách tìm khác, ví dụ package `com.tngtech.archunit`) | **Skipped** (2026-08-19, theo yêu cầu người dùng — không tiếp tục xác minh) |
| OI-AIRKI-12 | DDL gốc của `tbl_dim_ticket`/`tbl_dim_project`/`tbl_dim_repository` chưa từng được đọc trực tiếp (chỉ suy luận qua FK reference gián tiếp ở nơi khác) — `spec-pack.md` §14 tự nhận đây là gap. Cần đọc trực tiếp để xác nhận tên cột/kiểu dữ liệu FK chính xác trước khi viết migration `V511` thật. | DB | Đúng đắn FK của bảng mới | Medium | DB owner | Trước khi viết migration `V511` | **Resolved** (2026-08-19) |
| OI-AIRKI-13 | Chưa xác nhận đường dẫn file thật của trang PM Dashboard chính (FE) nơi cần compose component mới vào (nghi ngờ `PMDashboardPage.tsx`, chưa grep xác nhận), và chưa đọc `TemplateUsageByPhase.tsx` (mẫu tham chiếu) trực tiếp. | FE | Đúng vị trí compose component mới, tuân thủ AC Closure | Low | FE dev thực hiện impl | Trước khi code FE | **Resolved** (2026-08-19) |
| OI-AIRKI-14 | `review-checklist.md` AC-AIRKI-11 (Question): `spec-pack.md` §6/§9 quy định hiển thị FE dùng `CDataTable`; `impl-plan.md` (đối chiếu trực tiếp `TemplateUsageByPhase.tsx`/`SummaryCards.tsx`) lại đề xuất pattern card-grid kiểu `SummaryCards`. Hai tài liệu chưa được đồng bộ chính thức ở cấp AC — phát hiện lại trong lượt viết test (Task C, 2026-08-20), rà `open-issues.md` cũ không thấy entry nào theo dõi việc này. | Spec / FE | Cấu trúc component FE thật sự implement, và test FE (component + AC Closure) sẽ khẳng định cấu trúc nào — viết test trước khi chốt sẽ khóa cứng nhầm phương án | Medium | BA / FE lead | Trước khi viết/sửa test FE cho phần hiển thị `aiFindingStats` | **Resolved** (2026-08-20) |
| OI-AIRKI-15 | `review-checklist.md` dòng 68 (chưa xác định): giá trị parse được có numerator > denominator (vd. bảng §8 ghi `"3/2"`) — chưa rõ đây là dữ liệu bất thường cần log cảnh báo/loại bỏ (giống null-fallback của AC-AIRKI-4), hay chấp nhận lưu nguyên như file gốc (rate > 100%). Không tự ý thêm validate/test giả định một hướng — phát hiện lại khi rà test-plan.md trong lượt viết test (Task C, 2026-08-20). | Spec / Impl | Đúng đắn `AiReviewStatsParser`/`AiFindingStatsDto.rate()` với input bất thường, và test boundary tương ứng | Medium | BA / BE lead | Trước khi viết test boundary "numerator > denominator" | **Resolved** (2026-08-20) |
| OI-AIRKI-16 | `review-checklist.md` dòng 138 yêu cầu "Test migration verify `UNIQUE(ticket_id)` và FK constraint reject giá trị không hợp lệ", nhưng `rules/40-testing.md` cấm dùng production DB trong unit test và không có precedent integration-test-với-DB-thật cho migration khác trong ticket này. Constraint hiện chỉ được test gián tiếp qua SQL string assertion (`AiFindingStatJdbcAdapterTest`), không phải test thật chạy migration + insert vi phạm constraint. Cần quyết định: chấp nhận gap này (accepted risk), hay bổ sung 1 integration test riêng (Testcontainers/DB thật) nằm ngoài phạm vi unit test hiện tại? | Test / DB | Độ tin cậy thật của `UNIQUE(ticket_id)`/FK trước khi chạy migration `V511` trên production | Medium | DB owner / BE lead | Trước khi chạy `V511` trên môi trường thật | **Resolved** (2026-08-20) |

**Lưu ý về `ErrorResponse` shape**: đã đọc trực tiếp `web/exception/ErrorResponse.java`, xác nhận shape
thật là `(timestamp, status, error, message, traceId)` — 5 field, `error` là mã lỗi ngắn. Một số
standards khác (`api-contract.md`, `error-handling.md`) mô tả shape 6-field không khớp source thật —
đây là tài liệu sai lệch có sẵn, **không phải Open Issue của ticket này** (không cần sửa tài liệu đó
trong phạm vi ticket này, chỉ ghi nhận để không nhầm lẫn khi impl).

## Blockers

Không có blocker nào đang chặn việc bắt đầu implementation. Toàn bộ 13 Open Issue đã được xử lý:
OI-AIRKI-1..8 xác nhận ngày 2026-08-18, OI-AIRKI-9/10/12/13 xác nhận/resolve ngày 2026-08-19,
OI-AIRKI-11 được người dùng chủ động skip (không xác minh thêm). Cả 4 Gate "Blocking" trong
`impl-plan.md` (tương ứng OI-AIRKI-9, 10, 12, và thiết kế `MarkdownParserCore`) nay đã đóng 4/4 —
Gate #4 được người dùng xác nhận trực tiếp ngày 2026-08-19: định vị bảng §8 theo **vị trí/thứ tự
dòng và cột thật trong file `ai-review.md`** (positional/structural, không theo tên tiêu đề đã dịch),
khớp với `A-AIRKI-1` đã ghi trong `spec-pack.md` §16. Không còn gate nào chặn implementation.

3 Open Issue phát sinh trong lượt viết test bổ sung (Task C, 2026-08-20) — OI-AIRKI-14 (CDataTable
vs card-grid), OI-AIRKI-15 (numerator > denominator), OI-AIRKI-16 (migration constraint test gap) —
đã được người dùng resolve cùng ngày, xem Resolution Log. Tổng 16/16 Open Issue đã xử lý.

## Questions

_Không còn câu hỏi nào đang chờ — toàn bộ đã được trả lời, xem Resolution Log._

## Pending Human Decisions

_Không còn quyết định nào đang chờ (Pending) — cả 4 Human Decision gốc (H-AIRKI-1..4), 4 điểm bổ
sung được ủy quyền cho SDD analyst (H-AIRKI-5..8), quyết định giữ nguyên `A-AIRKI-3`
(OI-AIRKI-10, 2026-08-19), và xác nhận thiết kế positional/structural cho `MarkdownParserCore`
(Gate #4, 2026-08-19, khớp `A-AIRKI-1`) đã được xác nhận. Xem `spec-pack.md` §18 (Decision Log) để
tra cứu chi tiết từng quyết định và nơi áp dụng._

## Accepted but Unresolved Items

_Không còn mục nào ở trạng thái này. OI-AIRKI-11 (ArchUnit `ArchitectureTest.java` tồn tại hay
không) được người dùng chủ động chỉ định **skip** ngày 2026-08-19 — không phải "chưa resolve", mà là
quyết định có chủ đích không tiếp tục xác minh; nếu file này thực sự không tồn tại, code mới sẽ
không có safety net ArchUnit tự động, rủi ro được chấp nhận có ý thức._

## Resolution Log

| Ngày | ID | Câu trả lời của người dùng | Ghi chú áp dụng |
|---|---|---|---|
| 2026-08-18 | OI-AIRKI-1 | "Cứ xử lý như dòng đó tổng hợp cho Blocker/Major đi, tôi sẽ yêu cầu xử lý output của ai-review sau" | Cột `blocker_major_resolved_count`/`blocker_major_total_count` xử lý là số liệu tổng hợp chung Blocker+Major ở phiên bản hiện tại; việc tách riêng sẽ do 1 thay đổi khác đối với `ai-review.md` sau này |
| 2026-08-18 | OI-AIRKI-2 | "bắt buộc repositoryId" | API `ai-finding-stats` bắt buộc `repositoryId`, trả về 1 object/lần gọi (giống `template-usage`) |
| 2026-08-18 | OI-AIRKI-3 | "không cần" | Không hỗ trợ `periodKey` filter ở v1; giữ thiết kế cumulative snapshot mới nhất |
| 2026-08-18 | OI-AIRKI-4 | "tối giản" | `tbl_fact_ai_finding_stat` dùng schema tối giản kiểu V510 (chỉ `created_at`/`updated_at`), không audit/soft-delete đầy đủ |
| 2026-08-18 | OI-AIRKI-5 | "hãy quyết định làm sao cho đúng yêu cầu là được" (ủy quyền SDD analyst) | Giữ vị trí hook ngay sau vòng lặp build `ticketScopes` (dòng ~296) theo đúng phân tích kỹ thuật đã có, không thay đổi thứ tự các bước khác trong `handlePullRequest` |
| 2026-08-18 | OI-AIRKI-6 | "hãy quyết định làm sao cho đúng yêu cầu là được" (ủy quyền SDD analyst) | Bắt buộc đọc toàn bộ 33 test case `GithubWebhookServiceTest.java` + 3 file test PM Dashboard hiện có trước khi viết code/test mới trong impl-plan |
| 2026-08-18 | OI-AIRKI-7 | "null chỉ xảy ra khi tất cả ticket đều không có số liệu thôi đúng không, lúc đó hãy thể hiện là -" | Xác nhận: rate = null chỉ khi tổng denominator = 0 trên toàn bộ ticket trong scope; FE hiển thị `-` |
| 2026-08-18 | OI-AIRKI-8 | "không cần, tính năng của ứng dụng sau khi hoàn tất sẽ áp dụng ngay khi bắt đầu dự án" | Không backfill ticket cũ; chấp nhận gap dữ liệu trước thời điểm go-live |
| 2026-08-19 | OI-AIRKI-9 | "Các api khác đang check như nào thì hãy check giống như vậy" | Đã grep toàn bộ `web/` — xác nhận `GlobalExceptionHandler` chỉ có 11 `@ExceptionHandler`, không có handler nào cho `MissingServletRequestParameterException` ở bất kỳ nơi nào trong codebase (không riêng `template-usage`). Mọi endpoint yêu cầu `@RequestParam UUID` bắt buộc hiện tại đều nhận 500 khi thiếu param — đây là convention nhất quán 100% của toàn hệ thống, không phải ngoại lệ riêng của `template-usage`. Quyết định: endpoint mới **giữ nguyên hành vi 500 hiện tại**, không thêm handler mới, không mở rộng `GlobalExceptionHandler` — đúng theo đúng những gì các API khác đang làm |
| 2026-08-19 | OI-AIRKI-10 | "Cứ quyết như vậy đi" | Xác nhận giữ nguyên `A-AIRKI-3`: cột `ai_review_finding_total_count` là mẫu số dùng chung cho 4/5 KPI (adoption/valid/false-positive/resolution rate), `blocker_major_total_count` là mẫu số riêng cho Blocker/Major Resolution Rate. Giả định này nay được nâng cấp thành quyết định đã xác nhận (không còn là suy đoán AI chưa hỏi) — DDL `V512` (đổi từ `V511` ngày 2026-08-20, xem Resolution Log dòng dưới) dùng nguyên cấu trúc cột này |
| 2026-08-19 | OI-AIRKI-11 | "skip" | Không tiếp tục xác minh sự tồn tại của `ArchitectureTest.java`. Ghi nhận: nếu file không tồn tại, code mới sẽ không có safety net ArchUnit tự động cho vi phạm layer boundary — rủi ro được chấp nhận có chủ đích, không chặn implementation |
| 2026-08-19 | OI-AIRKI-12 | "chưa đọc thì hãy đọc đi" | Đã đọc trực tiếp `V4__init_shema_v2.sql` dòng 107-193 — xác nhận DDL đầy đủ của `tbl_dim_project` (PK `project_id` UUID), `tbl_dim_repository` (PK `repository_id` UUID, FK `project_id`, cột `repo_name_masked`), `tbl_dim_ticket` (PK `ticket_id` UUID, FK `project_id`, **không có cột `repository_id`**). Phát hiện quan trọng: `tbl_dim_ticket` không có liên kết trực tiếp tới repository — nhưng đã xác nhận (grep `GithubWebhookService.java`) rằng `repository.get().projectId()`/`repository.get().repositoryId()` đã sẵn có trong scope xuyên suốt `handlePullRequest` (dùng ở dòng 183/210/255/309...), bao gồm đúng tại vị trí chèn hook mới (~296-298) — nên writer mới lấy `project_id`/`repository_id` trực tiếp từ biến `repository` đã resolve sẵn của webhook, KHÔNG cần join qua `tbl_dim_ticket` để suy ra repository. Không phát sinh gap DDL nào cần xử lý thêm |
| 2026-08-19 | OI-AIRKI-13 | "chưa đọc thì hãy đọc đi" | Đã đọc `PMDashboardPage.tsx` (xác nhận đường dẫn thật, path đúng như suy đoán), `TemplateUsageByPhase.tsx`, và `SummaryCards.tsx`. Phát hiện quan trọng: `TemplateUsageByPhase.tsx` hiển thị **danh sách hàng** qua `CDataTable` (nhận props `rows: [...]`) — không phù hợp làm mẫu cho response **1 object duy nhất** của endpoint mới. `SummaryCards.tsx` mới là mẫu FE phù hợp hơn: nhận 1 object (`PmDashboardSummary \| undefined`), render dạng lưới thẻ KPI đơn giá trị (`{title, description, value}`) — khớp đúng hình dạng 5 KPI rate của ticket này. Xác nhận pattern compose: `useQuery({queryKey: [...], queryFn: () => endpoints.pmDashboard.X(...), enabled: Boolean(filters.projectId) && Boolean(filters.repositoryId)})` rồi render component ngay dưới `<SummaryCards>`/`<AllTicketsTable>` trong `PMDashboardPage.tsx`. Cập nhật: component mới nên đặt tên/cấu trúc theo mẫu `SummaryCards`, không theo mẫu `TemplateUsageByPhase` |
| 2026-08-19 | Gate #4 (impl-plan.md) | "Bạn đang còn thắc mắc gì của gate 4, tôi nói là nó lấy chính xác vị trí, thứ tự dòng, cột của bảng trong file ai-review.md rồi mà" | Xác nhận thiết kế `MarkdownParserCore` cho việc đọc bảng §8: định vị **theo vị trí/thứ tự dòng và cột thật trong bảng của `ai-review.md`** (positional/structural mapping) — heading tìm theo số thứ tự "8." (không theo tên tiêu đề dịch), từng dòng KPI lấy theo đúng thứ tự cố định (không match theo nhãn/label text), từng cột numerator/denominator lấy theo đúng vị trí cột cố định. Khớp hoàn toàn với `A-AIRKI-1` đã ghi trong `spec-pack.md` §16 — không phát sinh thiết kế mới, chỉ là xác nhận lại rõ ràng bằng lời của người dùng. Gate #4 trong `impl-plan.md` đóng — không còn gate nào chặn implementation |
| 2026-08-20 | OI-AIRKI-14 | "dùng SummaryCards" | Chốt chính thức mâu thuẫn AC-AIRKI-11: FE dùng pattern card-grid kiểu `SummaryCards` (không phải `CDataTable`) — khớp với phân tích kỹ thuật đã ghi ở OI-AIRKI-13 (2026-08-19). `spec-pack.md` §6/§9 (đề cập `CDataTable`) là tài liệu chưa đồng bộ, không sửa lại trong phạm vi ticket này. Đã cập nhật `review-checklist.md` AC-AIRKI-11 từ `Question` sang `Question (Resolved)` |
| 2026-08-20 | OI-AIRKI-15 | "Hãy xử lý với giả định data đầu vào không có chuyện 3/2" | Xác nhận giả định: input thực tế (bảng §8 của `ai-review.md`) không bao giờ có numerator > denominator. Không thêm validate/log-cảnh-báo cho trường hợp này trong `AiReviewStatsParser`/`AiFindingStatsDto.rate()`, không thêm test boundary cho input bất thường giả định không xảy ra. Đã đánh dấu `[x]` mục tương ứng trong `review-checklist.md` |
| 2026-08-20 | OI-AIRKI-16 | "Chấp nhận gap này" | Chấp nhận không viết integration test DB thật cho `UNIQUE(ticket_id)`/FK constraint của migration `V512` (đổi từ `V511` cùng ngày, xem dòng dưới) — nhất quán với rule cấm production DB trong unit test (`rules/40-testing.md`); constraint chỉ được xác nhận gián tiếp qua SQL string assertion ở `AiFindingStatJdbcAdapterTest`. Đã đánh dấu `[x]` mục tương ứng trong `review-checklist.md`, ghi nhận là rủi ro chấp nhận có ý thức |
| 2026-08-20 | (không có ID — sự cố hạ tầng, không phải Open Issue phát sinh từ spec) | "Tôi vừa merge nhánh main vào nên đã có v511 của người khác làm, hãy đổi V511__add_ai_finding_stat_tracking.sql lại" | Merge `main` làm lộ va version: `V511` đã bị 1 ticket khác chiếm bởi `V511__ai_quality.sql` (tạo `tbl_dim_ai_quality`, không liên quan/không đụng bảng của ticket này). Đổi file migration của ticket này thành `V512__add_ai_finding_stat_tracking.sql` (đã xác nhận `V512` còn trống tại thời điểm đổi). Cập nhật theo tên mới ở `spec-pack.md`, `self-review.md`, `impact-analysis.md`, `source-map.md`, `review-checklist.md`, dòng ngay trên. Không sửa lại các artifact review đã đóng dấu thời gian trước thời điểm này (`codex-review.md`, `test-results.md`, `blackbox-test-results.md`, `impl-plan.md`, `context.md`, `ticket-rules.md`, `test-plan.md`) — các file đó giữ nguyên `V511` như bản ghi lịch sử đúng tại thời điểm được viết |
